import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Info = () => {
  return (
    <SafeAreaView>
      <Text>Info</Text>
    </SafeAreaView>
  );
};

export default Info;
